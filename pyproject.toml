[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "fates-users-guide"
authors = [{name = "FATES Development Team", email = "glemieux@lbl.gov"}]
dynamic = ["version", "description"]
